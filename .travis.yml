language: node_js

node_js: '9.5.0'

addons:
  sonarqube:
    organization: "schnatterer-github"

cache:
    directories:
      - "node_modules"
      - '$HOME/.sonar/cache'
    yarn: true

install:
  - yarn install

script:
  - yarn package
  - sonar-scanner

